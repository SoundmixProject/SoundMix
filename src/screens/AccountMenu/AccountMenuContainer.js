/**
 * @format
 * @flow
 */

import React from 'react';
import { type Props } from './types';
import AccountMenuView from './AccountMenuView';

const AccountMenuContainer = (props: Props) => {
  return <AccountMenuView {...props} />;
};

export default AccountMenuContainer;
