/**
 * @format
 * @flow
 */

import React from 'react';
import AccountMenuContainer from './AccountMenuContainer';
import { type Props } from './types';

const AccountMenu = (props: Props) => {
  return <AccountMenuContainer {...props} />;
};

export default AccountMenu;
