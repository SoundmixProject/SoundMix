/**
 * @format
 * @flow
 */

import React from 'react';
import TrendingContainer from './TrendingContainer';
import { type Props } from './types';

const Trending = (props: Props) => {
  return <TrendingContainer {...props} />;
};

export default Trending;
