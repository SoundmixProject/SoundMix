/**
 *
 * @format
 * @flow
 */

import React from 'react';
import SignUpSucessPresenter from './presenter';
import { type Props } from './typed';

const SignUpSucess = (props: Props) => {
  return <SignUpSucessPresenter {...props} />;
};

export default SignUpSucess;
