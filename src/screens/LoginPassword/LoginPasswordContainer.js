/**
 * @format
 * @flow
 */

import React from 'react';
import { type Props } from './types';
import LoginPasswordView from './LoginPasswordView';

const LoginPasswordContainer = (props: Props) => {
  return <LoginPasswordView {...props} />;
};

export default LoginPasswordContainer;
