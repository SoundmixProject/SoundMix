/**
 * @format
 * @flow
 */

import React from 'react';
import PlayingContainer from './PlayingContainer';
import { type Props } from './types';

const Playing = (props: Props) => {
  return <PlayingContainer {...props} />;
};

export default Playing;
