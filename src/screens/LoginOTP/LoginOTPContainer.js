/**
 * @format
 * @flow
 */

import React from 'react';
import { type Props } from './types';
import LoginOTPView from './LoginOTPView';

const LoginOTPContainer = (props: Props) => {
  return <LoginOTPView {...props} />;
};

export default LoginOTPContainer;
