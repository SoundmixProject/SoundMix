/**
 * @format
 * @flow
 */

import React from 'react';
import LoginOTPContainer from './LoginOTPContainer';
import { type Props } from './types';

const LoginOTP = (props: Props) => {
  return <LoginOTPContainer {...props} />;
};

export default LoginOTP;
