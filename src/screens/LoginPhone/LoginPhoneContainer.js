/**
 * @format
 * @flow
 */

import React from 'react';
import { type Props } from './types';
import LoginPhoneView from './LoginPhoneView';

const LoginPhoneContainer = (props: Props) => {
  return <LoginPhoneView {...props} />;
};

export default LoginPhoneContainer;
