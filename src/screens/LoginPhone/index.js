/**
 * @format
 * @flow
 */

import React from 'react';
import LoginPhoneContainer from './LoginPhoneContainer';
import { type Props } from './types';

const LoginPhone = (props: Props) => {
  return <LoginPhoneContainer {...props} />;
};

export default LoginPhone;
