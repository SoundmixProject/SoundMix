/**
 * @format
 * @flow
 */

import React from 'react';
import AlbumsContainer from './AlbumsContainer';
import { type Props } from './types';

const Albums = (props: Props) => {
  return <AlbumsContainer {...props} />;
};

export default Albums;
